# Optional explicit backend host for chat API.
# Leave empty in production to use same-origin (/api) via reverse proxy.
# Example local direct call: http://localhost:3000
VITE_API_URL=

# Base URL for content API.
# Leave empty in production to use same-origin (/api) via reverse proxy.
# Example local direct call: http://localhost:3000
VITE_CONTENT_API_BASE=

# Optional SSO login URLs for user auth buttons.
# Example: https://curhatinai.com/api/auth/google/start
VITE_SSO_GOOGLE_URL=
# Example: https://curhatinai.com/api/auth/github/start
VITE_SSO_GITHUB_URL=
