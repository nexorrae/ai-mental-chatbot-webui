# AI Mental Chatbot WebUI - Environment Configuration
# Copy this file to .env and adjust values as needed

# Frontend port (default: 3001)
FRONTEND_PORT=3001

# Nginx port (default: 80)
NGINX_PORT=80

# Backend API URL
# Preferred in production: leave empty so frontend calls same-origin /api via reverse proxy.
# Local direct example (without reverse proxy): http://localhost:3000
VITE_API_URL=

# Content API base URL for articles CMS.
# Preferred in production: leave empty so frontend calls same-origin /api via reverse proxy.
# Local direct example (without reverse proxy): http://localhost:3000
VITE_CONTENT_API_BASE=

# Optional SSO endpoints for user login buttons in chat page.
# Fill with backend OAuth start endpoint if enabled, otherwise leave empty.
# Example: https://curhatinai.com/api/auth/google/start
VITE_SSO_GOOGLE_URL=
# Example: https://curhatinai.com/api/auth/github/start
VITE_SSO_GITHUB_URL=
